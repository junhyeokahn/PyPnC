name: pypnc
channels:
  - anaconda
  - conda-forge
dependencies:
  - libcblas=3.9.0
  - liblapack=3.9.0
  - pinocchio=2.5.5
  - python=3.8
  - ros-rospy
  - ros-sensor-msgs
  - ros-geometry-msgs
#  For some reason only ros-control-msgs out of the ros messages we want to use doesn't
#   exist as a conda-forge package. Built and copied manually into this repo.
#  TODO: can we get around this?
#  - ros-control-msgs
  - pip
  - pip:
      - casadi==3.5.5
      - conda-env-export
      - flatbuffers==1.12
      - imageio==2.9.0
      - ipdb==0.13.4
      - matplotlib==3.3.3
      - meshcat==0.0.19
      - opencv-python==4.5.1.48
      - pybullet==3.0.8
      - qpsolvers==1.7.1
      - ruamel.yaml==0.16.12
      - tensorflow==2.3.0
      - tqdm==4.56.0
      - yapf==0.30.0
      - open3d
# Dependency issues using tf2-ros package alongside python3.8 and other listed dependencies
# Broadcasting camera transform separately for now, but if we really need tf integration
#   then figure out dependency conflicts or explore building from source
#      - tf2-ros
prefix: /home/seunghyeonbang/anaconda3/envs/pypnc
